---
import { apiClient } from "@/lib/api/api-client";
import BlogLayout from "@/layouts/blog-layout.astro";

const slug = Astro.params.slug;
const about = await apiClient
	.collection("pages")
	.getFirstListItem(`slug="${slug}"`);
---

<BlogLayout>
	<div set:html={about.content} />
</BlogLayout>
