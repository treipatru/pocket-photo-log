---
import { pb } from "@/lib/pb";
import BlogLayout from "@/layouts/blog-layout.astro";

const slug = Astro.params.slug;
const about = await pb.collection("pages").getFirstListItem(`slug="${slug}"`);
---

<BlogLayout>
	<div set:html={about.content} />
</BlogLayout>
